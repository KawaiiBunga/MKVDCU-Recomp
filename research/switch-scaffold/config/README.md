# Configuration boundary

Store target-neutral platform options here. Per-target executable hashes, codegen config, paths, patches, and generated source belong under ignored `targets/<target>/` locations.

